---
title: Counterfactual Regret Minimization for Poker Variants
date: 2025-11-04 20:00:00 -0500
categories: [game theory, algorithms]
tags: [game theory, poker, cfr, nash equilibrium, algorithm]
math: true
---

## Introduction

Counterfactual Regret Minimization (CFR) is a foundational algorithm for computing Nash equilibria in imperfect information games. CFR iteratively improves strategies through regret minimization, playing through the game tree over and over and updating a learned strategy each time.

In this post, we explore CFR through the lens of simple poker variants—from Kuhn poker (the simplest non-trivial imperfect information game) to more strategic variants like von Neumann poker and Limit Continuous Poker (LCP). We then discuss modern extensions to vanilla CFR that improve convergence and explore strategic properties.

## Vanilla CFR

### Core Concept

CFR is based on a simple principle: **play actions with positive regret more often**. Regret for an action is how much better we would have done if we'd played it instead of our current mixed strategy.

The algorithm maintains two key quantities for each decision node:
- **Cumulative regret**: Sum of regrets for each action across all iterations
- **Strategy**: Proportional to positive cumulative regrets (or uniform if all negative)

Formally, for each information set $I$ and action $a$, we track:

$$R^T(I, a) = \sum_{t=1}^{T} \left( v^t(I, a) - v^t(I, \sigma^t) \right)$$

where $v^t(I, a)$ is the counterfactual value of always playing action $a$ at $I$, and $v^t(I, \sigma^t)$ is the counterfactual value of the current strategy. The strategy at iteration $T+1$ is then:

$$\sigma^{T+1}(I, a) = \frac{\max(R^T(I, a), 0)}{\sum_{a'} \max(R^T(I, a'), 0)}$$

If all regrets are non-positive, we play uniformly. The average strategy $\bar{\sigma}^T$ converges to a Nash equilibrium as $T \to \infty$.

### Kuhn Poker

Kuhn poker is the simplest imperfect information game: each player receives one of three cards (Jack, Queen, King), with equal probability. One player bets (or checks), the other calls (or folds). Higher card wins.

This 12-node game tree is tiny by modern standards, yet it's a genuine test of CFR:
- Players don't observe opponent's cards (imperfect information)
- Players have real strategic choices (check/bet, call/fold)
- The Nash equilibrium is known and simple: check-call all hands except bluff with J

We can define Kuhn poker and run CFR using the game_utils package:
```python
from game_utils2.core.game import SequentialGame
from game_utils2.core.strategy import Strategy, StrategyProfile
from game_utils2.solvers.CFR import CFRSolver

kuhn = SequentialGame(
    actions=lambda hist: ["bet", "check"] if len(hist) == 0 else ["call", "fold"],
    player=lambda hist: len(hist) % 2,
    types=lambda: tuple(np.random.choice([0, 1, 2], size=2, replace=False)),
    terminal=lambda hist: len(hist) > 0 and hist[-1] in ['check', 'call', 'fold'],
    payoff=lambda hist, card0, card1: (
        0.5 if hist[-1] == 'fold' else
        1.5 * (1 if card0 > card1 else -1) if hist[-1] == 'call' else
        0.5 * (1 if card0 > card1 else -1)
    )
)

kuhnSolver = CFRSolver(kuhn, lambda_reg=0)
kuhnSolver.train(10000)
kuhnStrategy = kuhnSolver.get_strategy()

info_sets = kuhnSolver.get_info_sets()
plot_cfr_regret_timeline(kuhnSolver)
strategy_heatmaps(kuhnStrategy, info_sets, annotate=True)
```
![Kuhn Training Regret](/assets/img/cfr/kuhn_training.png)
![Kuhn Learned Strategy](/assets/img/cfr/kuhn_strategy.png)

This strategy indeed lines up with the known Nash equilibrium which can be found online.

### Von Neumann Poker

Von Neumann poker introduces continuous hand strengths uniformly drawn from [0, 1]—much richer than Kuhn's three-card discrete game.

CFR on continuous games requires **discretization**: partition [0, 1] into buckets (e.g., 100 or 1000 equally-sized intervals). Each bucket is treated as a separate decision node. This effectively reduces the game back to Kuhn with many more cards, but this is a good starting point for studying the continuous game. 

```python
def FBCP(bet_size, type_intervals): 
    return SequentialGame(
        actions=lambda hist: ["bet", "check"] if len(hist)==0 else ["call", "fold"],
        player=lambda hist: len(hist) % 2,
        types=lambda: tuple(np.random.choice(np.linspace(0, 1, type_intervals), size=2, replace=False)),
        terminal=lambda hist: len(hist) > 0 and hist[-1] in ['check', 'call', 'fold'],
        payoff=lambda hist, card0, card1: (
            0.5 if hist[-1] == 'fold' else
            (bet_size + 0.5) * (1 if card0 > card1 else -1) if hist[-1] == 'call' else
            0.5 * (1 if card0 > card1 else -1)
        )
    )

FBCPSolver = CFRSolver(FBCP(1, type_intervals=51))
FBCPSolver.train(100000)
FBCPStrategy = FBCPSolver.get_strategy()
plot_cfr_regret_timeline(FBCPSolver, log_scale=True)
strategy_heatmaps(FBCPStrategy, FBCPSolver.get_info_sets(), annotate=False)
```

![Von Neumann Training Regret](/assets/img/cfr/fbcp_training.png)
![Von Neumann Learned Strategy](/assets/img/cfr/fbcp_strategy.png)

Von Neumann poker has a known pure Nash equilibrium where the caller calls above some hand strength cutoff and the bettor checks intermediate hands, betting below and above some thresholds. The learned strategy gets close to a this Nash Equilibrium but does not find the clean pure strategy. This is partially because there are regions where the exact hand strength is not important - namely, the caller has no incentive to use a sharp cutoff because the bettor checks hands around the calling cutoff anyway. In other words, this strategy is not suboptimal, it just isn't the simplest interpretable equilibrium. This is a common theme for CFR - we will often find some optimal strategy profile, but we get no guarantee on how 'clean' or 'messy' it is.

### Limit Continuous Poker (LCP)

LCP allows bet sizing in the range $[L, U]$. This adds continuous action spaces on top of continuous hand strengths.

We can take a similar approach by discretizing actions: partition $[L, U]$ into M bet sizes. Now we have $N \times M$ decision nodes (N hand strength buckets, M bet size options).

```python

def LCP(L, U, type_intervals, bet_size_intervals): 
    return SequentialGame(
        actions=lambda hist: np.concat([[0], np.linspace(L, U, bet_size_intervals)]) if len(hist)==0 else ["call", "fold"],
        player=lambda hist: len(hist) % 2,
        types=lambda: tuple(np.random.choice(np.linspace(0, 1, type_intervals), size=2, replace=False)),
        terminal=lambda hist: False if (len(hist) == 0) else (len(hist)== 2 or hist[0] == 0),
        payoff=lambda hist, x, y: (
            0.5 if hist[-1] == 'fold' else
            (hist[0] + 0.5) * (1 if x > y else -1) if hist[-1] == 'call' else
            0.5 * (1 if x > y else -1)
        )
    )


L, U = 0.5, 1.5
game = LCP(L, U, type_intervals=51, bet_size_intervals=11)
LCPSolver = CFRSolver(game)

LCPSolver.train(50000)
strategy = LCPSolver.get_strategy()

info_sets = LCPSolver.get_info_sets()
betting = [(h, t) for h, t in info_sets if len(h)==0]
strategy_heatmaps(strategy, betting, annotate=False)
calling = [(h, t) for h, t in info_sets if len(h)==1 and h!=(0)]
calling_freq_heatmap(strategy, calling)
plot_cfr_regret_timeline(LCPSolver, log_scale=True)
```
![LCP Training Regret](/assets/img/cfr/lcp_training.png)
![LCP Learned Betting Strategy](/assets/img/cfr/lcp_betting.png)
![LCP Learned Calling Strategy](/assets/img/cfr/lcp_calling.png)

For reference, the pure strategy NEQ for LCP looks something like this:
![LCP Pure NEQ](/assets/img/lcp/LCP_profile_0.3_1.5.png)

We see something moderately similar in shape: intermediate hand strengths always check, extremes bet, and the calling strategy roughly resembles the shape we expect. However, the bluffing strategy is all over the place and the calling strategy does not find anything close to a sharp cutoff curve. This actually aligns well with our knowledge of LCP - if we just want any NEQ, the hand strength of bluffs is irrelevant as long as they balance value bets to give the caller the right pot odds, and the hand strength of calling hands only matters so far as to keep the better indifferent from betting more or less.

The learned strategy actually mixes bluffs so uniformly that it is unclear from this plot whether it balances value bets at all, but we can check for a specific bet sizes by taking a cross section of this plot as a bar chart:
![Likelihood of Bet Across Hand Strengths](/assets/img/cfr/cross_section.png)
This represents the likelihood of a specific bet size (1.1) given each hand strength the bettor might have. The caller wants to use this to predict the bettors hand strength. If we think of this from the caller's perspective and use a uniform prior on the bettor's hand strength, then this is exactly proportional to the posterior, that is, the distribution of hand strengths given the bet size. To be well-balanced, the ratio of areas right to left of the calling cutoff must equal the ratio of payoffs (in this case, 1.6 : 1.1). It turns out that the strategy is roughly balanced, even if the bluffing hands are seemingly randomly distributed.

## CFR Variants and Enhancements

Vanilla CFR converges, but it can be slow on complex games and doesn't give any guarantee on which equilibrium it converges to if there are multiple. In our case, we know that there exists a pure strategy NEQ, but vanilla CFR converges to a mixed NEQ which is difficult to interpret.There are several variants we can explore to address these issues.

### 1. CFR with Exploration Factor

**Motivation**: In early iterations, strategies can be very lopsided (high regret on one action). This causes poor exploration—we barely sample suboptimal actions and learn little about their true counterfactual values.

**Idea**: Add a small, uniform exploration bonus to all actions, ensuring we sample the full strategy space early.

We modify the strategy used during traversal (not the one we're learning) by mixing in uniform exploration:

$$\sigma_{\text{sample}}(I, a) = (1 - \epsilon) \cdot \sigma(I, a) + \epsilon \cdot \frac{1}{\lvert A(I) \rvert}$$

where $\epsilon \in (0, 1)$ is the exploration parameter. This ensures every action is sampled with probability at least $\frac{\epsilon}{\lvert A(I) \rvert}$, preventing premature convergence to suboptimal regions of strategy space. As training progresses, $\epsilon$ can be annealed toward zero.

In LCP specifically, we see that the learned calling strategy does not have strong continuity in the sense that we don't get a smooth gradient across hand strengths and betting sizes. The theoretical strategy we found is monotone (larger bets are only called by stronger hands), but in practice, we see very choppy behavior. One possibility is that the algorithm is simply not reaching scenarios which require it to learn these parameters.

We can try this technique on LCP with $\epsilon=0.05$:

![LCP Training Regret with Exploration](/assets/img/cfr/lcp_training.png)
![LCP Learned Betting Strategy with Exploration](/assets/img/cfr/lcp_betting_explore.png)
![LCP Learned Calling Strategy with Exploration](/assets/img/cfr/lcp_calling_explore.png)

It turns out that this doesn't have any noticeable effect, even with tweaking the value of $\epsilon$. In hindsight, this makes sense - the issue is not just that certain parts of the game tree aren't being reached, but rather that there is not actual reason for an optimal strategy to have this expected monotone behavior, as long as the strategy is balanced to prevent the opponent from exploiting it. Theoretical monotonicity comes from a different property related to admissibility, which we won't get into here. However, it would be nice to see if CFR can still learn this strategy by some other tweak to the algorithm.


### 2. Entropy Regularization

**Motivation**: CFR does not discriminate between different Nash equilibria. In some cases, want to specifically target either pure or mixed Nash equilibria.

**Idea**: Add an entropy term which rewards either pure or mixed strategies (depending on the sign). 

The entropy of a strategy (or a distribution in general) is a measure of how 'spread out' or 'mixed' it is. If we know we are looking for a pure NEQ, we can add an entropy penalty to the objective:

$$R^T_{\lambda}(I, a) = R^T(I, a) + \lambda \cdot \frac{\partial H(\sigma(I))}{\partial \sigma(I, a)} $$

where $H(\sigma) = -\sum_a \sigma(a) \log \sigma(a)$ is the entropy of the strategy at $I$. 

- $\lambda > 0$: rewards high-entropy (mixed) strategies, smoothing the equilibrium
- $\lambda < 0$: penalizes mixing, pushing toward pure strategies

This gives us a knob to bias CFR toward the type of equilibrium we prefer, though it slightly perturbs the game and the result is only an approximate Nash equilibrium of the original game.

We first test this regularization on FBCP as a proof of concept, using $\lambda = 0.1$ and $\lambda = -0.1$.

![Von Neumann Learned Strategy](/assets/img/cfr/fbcp_strategy_mixed.png)
![Von Neumann Learned Strategy](/assets/img/cfr/fbcp_strategy_pure.png)

Clearly, we get one strategy with a smooth gradient and one which is completely pure. In general, neither of these is necessarily a NEQ, since we intentionally changed our optimization target to incentivize something other than payoff. However, it should be a little concerning that we know a pure strategy NEQ exists for this game, but the learned pure strategy doesn't look like the one we expected. 

For LCP, we know there is a theoretical pure NEQ. We can try this technique with a more moderate $\lambda = -0.05$:

![LCP Training Regret with Entropy Regularization](/assets/img/cfr/lcp_training_reg.png)
![LCP Learned Betting Strategy with Entropy Regularization](/assets/img/cfr/lcp_betting_reg.png)
![LCP Learned Calling Strategy with Entropy Regularization](/assets/img/cfr/lcp_calling_reg.png)


Again, this doesn't work quite as hoped. In fact, we get even further from the nice interpretable strategy we expected.

This is the result of a more fundamental issue. By design, we are discretizing a game with continuous action and type spaces. We know that a pure NEQ exists, but we also know that it relies heavily on the continuous nature of the game. For example, in a pure strategy which balances value bets and bluffs, every bet size needs at least one corresponding value betting hand strengths and one bluffing hand strength. With continuous hand strengths, this is always possible because we have infinite hand strengths to allocate to these two groups. However, when we discretize, this is no longer the case - we have fewer bluffing hand strengths to begin with, so we have to mix to have a balanced strategy. Thus, CFR cannot learn an optimal pure strategy because the game we've given it does not have one.


## Conclusion

CFR is a remarkably general algorithm—it handles imperfect information, works on any finite extensive-form game, and comes with strong convergence guarantees. But as we've seen, "converges to a Nash equilibrium" leaves a lot of room for interpretation:

- The equilibrium may be mixed even when pure equilibria exist
- Strategies in regions of indifference can look noisy or arbitrary
- Discretization artifacts matter for continuous games

Extensions like exploration factors and entropy regularization give us tools to steer CFR toward more interpretable or strategically useful solutions. There is always a tradeoff between theoretical purity (exact Nash) and practical utility (clean, implementable strategies).

For real applications, understanding *which* equilibrium you're computing and exactly which game you are working with are important questions to consider before using a tool like this.
